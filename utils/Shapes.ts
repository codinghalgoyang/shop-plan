// Use only 3 FontWeight
// Not use light, ultraLight, etc.
export type FontWeight = "thin" | "normal" | "bold";

export type FontSize = "small" | "normal" | "big";

export const FONT_SIZE = {
  small: 14,
  normal: 18,
  big: 22,
};

export const ITEM_HEIGHT = 40;
